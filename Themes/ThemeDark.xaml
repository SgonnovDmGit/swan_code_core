<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===================== COLORS ===================== -->

    <SolidColorBrush x:Key="PrimaryBackground" Color="#10161D"/>
    <SolidColorBrush x:Key="SecondaryBackground" Color="#161E27"/>
    <SolidColorBrush x:Key="TertiaryBackground" Color="#1D2733"/>
    <SolidColorBrush x:Key="PrimaryForeground" Color="#E6EDF3"/>
    <SolidColorBrush x:Key="SecondaryForeground" Color="#8B98A5"/>
    <SolidColorBrush x:Key="AccentColor" Color="#6EE7BE"/>
    <SolidColorBrush x:Key="AccentHoverColor" Color="#8FF0CE"/>
    <SolidColorBrush x:Key="AccentPressedColor" Color="#5AD5AA"/>
    <SolidColorBrush x:Key="BorderColor" Color="#2A3642"/>
    <SolidColorBrush x:Key="ButtonBackground" Color="#1D2733"/>
    <SolidColorBrush x:Key="ButtonForeground" Color="#E6EDF3"/>
    <SolidColorBrush x:Key="ButtonHoverBackground" Color="#26323F"/>
    <SolidColorBrush x:Key="ButtonPressedBackground" Color="#33404D"/>
    <SolidColorBrush x:Key="TextBoxBackground" Color="#161E27"/>
    <SolidColorBrush x:Key="TextBoxBorder" Color="#33404D"/>
    <SolidColorBrush x:Key="TabActiveBackground" Color="#161E27"/>
    <SolidColorBrush x:Key="TabInactiveBackground" Color="#1D2733"/>
    <SolidColorBrush x:Key="DataGridHeaderBackground" Color="#1D2733"/>
    <SolidColorBrush x:Key="DataGridRowAltBackground" Color="#131A22"/>
    <SolidColorBrush x:Key="DataGridSelectedBackground" Color="#1C3A30"/>
    <SolidColorBrush x:Key="MenuBackground" Color="#1D2733"/>
    <SolidColorBrush x:Key="MenuHoverBackground" Color="#26323F"/>
    <SolidColorBrush x:Key="ScrollBarBackground" Color="#161E27"/>
    <SolidColorBrush x:Key="ScrollBarThumb" Color="#33404D"/>

    <!-- Chat & Diff -->
    <SolidColorBrush x:Key="UserBubbleBackground" Color="#1C3A30"/>
    <SolidColorBrush x:Key="AssistantBubbleBackground" Color="#1A2430"/>
    <SolidColorBrush x:Key="DiffAddedBackground" Color="#16301F"/>
    <SolidColorBrush x:Key="DiffRemovedBackground" Color="#33201D"/>
    <SolidColorBrush x:Key="StatusBarBackground" Color="#0B1015"/>
    <SolidColorBrush x:Key="StatusBarForeground" Color="#8B98A5"/>
    <SolidColorBrush x:Key="TitleBarBackground" Color="#0E1319"/>
    <SolidColorBrush x:Key="TitleBarForeground" Color="#E6EDF3"/>

    <!-- GroupBox -->
    <SolidColorBrush x:Key="GroupBoxBorderColor" Color="#2A3642"/>
    <SolidColorBrush x:Key="GroupBoxHeaderForeground" Color="#E6EDF3"/>

    <!-- LenSa v0.13.3: reasoning / tool cards / status colors -->
    <SolidColorBrush x:Key="AccentOnFill" Color="#10161D"/>
    <SolidColorBrush x:Key="UserBubbleBorder" Color="#25523F"/>
    <SolidColorBrush x:Key="ReasoningForeground" Color="#FFD83D"/>
    <SolidColorBrush x:Key="ReasoningBackground" Color="#14FFD83D"/>
    <SolidColorBrush x:Key="ReasoningBorder" Color="#40FFD83D"/>
    <!-- Поля ввода (T-000082) — темнее панели, «вдавленные» -->
    <SolidColorBrush x:Key="InputBackground" Color="#0D141B"/>
    <SolidColorBrush x:Key="InputBorder" Color="#3A4A59"/>
    <SolidColorBrush x:Key="InputReadOnlyBackground" Color="#101821"/>
    <SolidColorBrush x:Key="ToolCardBackground" Color="#151C24"/>
    <SolidColorBrush x:Key="ToolCardBorder" Color="#2A3642"/>
    <SolidColorBrush x:Key="SuccessColor" Color="#3FB68B"/>
    <SolidColorBrush x:Key="ErrorColor" Color="#E5534B"/>
    <SolidColorBrush x:Key="ErrorBackground" Color="#1AE5534B"/>

    <!-- Уровни размышлений (композер, мокап 2026-07-12): нарастание «серый → фиолетовый».
         Наружу виден только кружок этого цвета рядом с моделью.
         Максимум БЫЛ красным (#EF5350) — почти в точности ErrorColor (#E5534B): кружок читался
         как «ошибка» или «нет связи». Виноградный взят посветлее, чем в светлой теме, — 9-пиксельный
         кружок на тёмном фоне должен светиться. -->
    <SolidColorBrush x:Key="ReasoningOff" Color="#8D97A3"/>
    <SolidColorBrush x:Key="ReasoningMinimal" Color="#5FA3E8"/>
    <SolidColorBrush x:Key="ReasoningLow" Color="#4FB489"/>
    <SolidColorBrush x:Key="ReasoningMedium" Color="#E5A050"/>
    <SolidColorBrush x:Key="ReasoningHigh" Color="#CC5DE8"/>

    <!-- Режимы: цвет по «развязанности рук», а не по светофору.
         Ревью БЫЛО лиловым — оно роднилось с виноградным кружком максимума размышлений
         (чипы стоят вплотную). Взята бирюза — свободное семейство; янтарь не берём, он липнет
         к «Авто», а спутать Ревью с Авто дороже всего. -->
    <SolidColorBrush x:Key="ModePlanning" Color="#5FA3E8"/>
    <SolidColorBrush x:Key="ModePlanningBackground" Color="#1B2C3D"/>
    <SolidColorBrush x:Key="ModeReview" Color="#3BC9DB"/>
    <SolidColorBrush x:Key="ModeReviewBackground" Color="#14313A"/>
    <SolidColorBrush x:Key="ModeAuto" Color="#E0705C"/>
    <SolidColorBrush x:Key="ModeAutoBackground" Color="#3A231F"/>

    <!-- Заливка чипов статуса доски (см. Theme1C) -->
    <SolidColorBrush x:Key="StatusDoneBackground" Color="#1B3A2E"/>
    <SolidColorBrush x:Key="StatusProgressBackground" Color="#14303A"/>
    <SolidColorBrush x:Key="StatusBlockedBackground" Color="#3A1F1D"/>
    <SolidColorBrush x:Key="CodeBackground" Color="#0C1117"/>
    <SolidColorBrush x:Key="CodeBorder" Color="#232E3A"/>
    <SolidColorBrush x:Key="PopupBackground" Color="#1D2733"/>
    <SolidColorBrush x:Key="PopupBorder" Color="#33404D"/>

    <!-- ===================== STYLES ===================== -->

    <!-- GroupBox -->
    <Style TargetType="GroupBox">
        <Setter Property="Foreground" Value="{DynamicResource GroupBoxHeaderForeground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource GroupBoxBorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <!-- Button -->
    <Style TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource ButtonBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource ButtonForeground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="2"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource ButtonHoverBackground}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{DynamicResource ButtonPressedBackground}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- TextBox -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource TextBoxBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource TextBoxBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="CaretBrush" Value="{DynamicResource PrimaryForeground}"/>
        <Setter Property="SelectionBrush" Value="{DynamicResource AccentColor}"/>
    </Style>

    <!-- Label -->
    <Style TargetType="Label">
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <!-- CheckBox -->
    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <!-- TabControl -->
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{DynamicResource SecondaryBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
    </Style>

    <!-- TabItem -->
    <Style TargetType="TabItem">
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="TabItem">
                    <Border x:Name="tabBorder"
                            Background="{DynamicResource TabInactiveBackground}"
                            BorderBrush="{DynamicResource BorderColor}"
                            BorderThickness="1,1,1,0"
                            CornerRadius="4,4,0,0"
                            Padding="{TemplateBinding Padding}"
                            Margin="2,0,0,0">
                        <ContentPresenter x:Name="contentPresenter"
                                          ContentSource="Header"
                                          HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="tabBorder" Property="Background" Value="{DynamicResource TabActiveBackground}"/>
                            <Setter TargetName="tabBorder" Property="BorderThickness" Value="1,1,1,0"/>
                            <Setter TargetName="tabBorder" Property="Margin" Value="2,0,0,-1"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="tabBorder" Property="Background" Value="{DynamicResource ButtonHoverBackground}"/>
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsSelected" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="tabBorder" Property="Background" Value="{DynamicResource TabActiveBackground}"/>
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- DataGrid -->
    <Style TargetType="DataGrid">
        <Setter Property="Background" Value="{DynamicResource PrimaryBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
        <Setter Property="RowBackground" Value="{DynamicResource PrimaryBackground}"/>
        <Setter Property="AlternatingRowBackground" Value="{DynamicResource DataGridRowAltBackground}"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource BorderColor}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource BorderColor}"/>
        <Setter Property="GridLinesVisibility" Value="All"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <!-- DataGridColumnHeader -->
    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{DynamicResource DataGridHeaderBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <!-- DataGridCell -->
    <Style TargetType="DataGridCell">
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="4,2"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource DataGridSelectedBackground}"/>
                <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- DataGridRow -->
    <Style TargetType="DataGridRow">
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource DataGridSelectedBackground}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <!-- ComboBox ToggleButton -->
    <Style x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition Width="20"/>
                        </Grid.ColumnDefinitions>
                        <Border x:Name="Border" Grid.ColumnSpan="2"
                                Background="{DynamicResource TextBoxBackground}"
                                BorderBrush="{DynamicResource TextBoxBorder}"
                                BorderThickness="1" CornerRadius="2"/>
                        <Border Grid.Column="0" Margin="1"
                                Background="{DynamicResource TextBoxBackground}"/>
                        <Path x:Name="Arrow" Grid.Column="1"
                              Fill="{DynamicResource PrimaryForeground}"
                              HorizontalAlignment="Center" VerticalAlignment="Center"
                              Data="M0,0 L4,4 8,0"/>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBox -->
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{DynamicResource TextBoxBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource TextBoxBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
        <Setter Property="ScrollViewer.CanContentScroll" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <ControlTemplate.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent"/>
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#E6EDF3"/>
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Transparent"/>
                    </ControlTemplate.Resources>
                    <Grid>
                        <ToggleButton Style="{StaticResource ComboBoxToggleButton}"
                                      Focusable="False"
                                      IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"/>
                        <ContentPresenter x:Name="ContentSite"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="8,4,24,4"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Left"/>
                        <Popup x:Name="Popup" Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True" Focusable="False"
                               PopupAnimation="Slide">
                            <Grid x:Name="DropDown"
                                  SnapsToDevicePixels="True"
                                  MinWidth="{TemplateBinding ActualWidth}"
                                  MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border x:Name="DropDownBorder"
                                        Background="{DynamicResource SecondaryBackground}"
                                        BorderBrush="{DynamicResource BorderColor}"
                                        BorderThickness="1" CornerRadius="2">
                                    <ScrollViewer Margin="2" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ComboBoxItem -->
    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
        <Setter Property="Padding" Value="6,4"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Border" Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource ButtonHoverBackground}"/>
                        </Trigger>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource AccentColor}"/>
                            <Setter Property="Foreground" Value="{DynamicResource AccentOnFill}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ContextMenu -->
    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource MenuBackground}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderColor}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
    </Style>

    <!-- MenuItem -->
    <Style TargetType="MenuItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryForeground}"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="FontSize" Value="13"/>
        <Style.Triggers>
            <Trigger Property="IsHighlighted" Value="True">
                <Setter Property="Background" Value="{DynamicResource MenuHoverBackground}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
